# Block nouveau and Rust-based nova_core. Redundant with the kernel cmdline
# blacklists but harmless; defence in depth so any code path that consults
# modprobe.d also refuses to load these.

blacklist nouveau
blacklist nova_core
options nouveau modeset=0
